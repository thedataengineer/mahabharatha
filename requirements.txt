# Core dependencies
click>=8.0
pydantic>=2.0
pyyaml>=6.0
rich>=13.0

# Development dependencies
pytest>=7.0
pytest-cov>=4.0
pytest-asyncio>=0.21
mypy>=1.0
ruff>=0.1

# Optional: Token metrics (pip install zerg-ai[metrics])
# anthropic>=0.40.0
